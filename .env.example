# API Service Configuration
# Change PORT to any available port (default: 5000)
PORT=5001

# API Base URL (used by publisher and subscriber)
# Update this to match your PORT setting
API_URL=http://localhost:5001

# Optional: API Service Host (default: 0.0.0.0)
# HOST=0.0.0.0
